{% include 'default/_form.html.twig' with {
    page_title: 'title_comment_create',
    attr: {
        method: 'POST',
        action: url('comment_create', {'id': article.id}),
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'article_index',
} %}