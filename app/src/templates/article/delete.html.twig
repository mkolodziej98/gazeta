{#{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}#}
    {% include 'default/_form.html.twig' with {
        page_title: 'title_article_delete',
        page_title_param: {'%id%': article.id},
        attr: {
            method: 'DELETE',
            action: url('article_delete', {'id': article.id}),
        },
        form: form,
        submit_label: 'action_delete'|trans,
        back_to_list_path: 'article_index',
    } %}
{#{% else %}#}
{#    <div>#}
{#        <a href="{{ url('app_login') }}">{{ 'action_login'|trans }}</a>#}
{#    </div>#}
{#{% endif %}#}